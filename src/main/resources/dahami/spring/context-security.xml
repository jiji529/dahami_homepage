<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"	
	xsi:schemaLocation=
		"http://www.springframework.org/schema/beans 
		http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/security
		http://www.springframework.org/schema/security/spring-security.xsd">

 	<beans:bean id="jdbcUserService" class="org.springframework.security.provisioning.JdbcUserDetailsManager">
		<beans:property name="dataSource" ref="dahami" />
		<beans:property name="enableGroups" value="false" />
		<beans:property name="enableAuthorities" value="true" />
		<beans:property name="usersByUsernameQuery">
			<beans:value>
				SELECT id AS USER_ID, pw AS PASSWORD, 1 AS ENABLED FROM member_info WHERE id=?
			</beans:value>			
		</beans:property>
		<beans:property name="authoritiesByUsernameQuery">
			<beans:value>
				SELECT id AS USERNAME, division AS AUTHORITY FROM member_info WHERE id=?
			</beans:value>
		</beans:property>
	</beans:bean>
	
	<beans:bean id="passwordEncoder" class="org.springframework.security.authentication.encoding.ShaPasswordEncoder">
		<beans:constructor-arg name="strength" value="512"></beans:constructor-arg>
	</beans:bean>
	
	<http auto-config="true" use-expressions="true">
		<intercept-url pattern="/lgn/login.html" access="permitAll" />
		<intercept-url pattern="/lgn/loginfailed.html" access="permitAll" />
		<intercept-url pattern="/**" access="permitAll" />
		
		<intercept-url pattern="/favicon.ico" access="permitAll"/>
		<!-- dssung -->
		<intercept-url pattern="/image/**" access="permitAll"/>
		<intercept-url pattern="/css/**" access="permitAll"/>
		
		<intercept-url pattern="/mypage/**" access="hasRole('ROLE_USER')" />
			
		<form-login login-page="/lgn/login.html" default-target-url="/"	authentication-failure-url="/lgn/loginfailed.html" />				
		<logout invalidate-session="true" logout-success-url="/" logout-url="/j_spring_security_logout"/>

		<remember-me key="gyp;goaDS43teEm88EurO" />
		
		<session-management>
			<concurrency-control max-sessions="10" expired-url="/lgn/login.html"/>
		</session-management>
	</http>
	
	<authentication-manager alias="authenticationManager">
		<authentication-provider user-service-ref="jdbcUserService">
			<password-encoder hash="sha-256" />
		</authentication-provider>
	</authentication-manager>
</beans:beans>
